aliyun:
  oss:
    # SDK 上传用。不带协议头时 SDK 按 http 处理，可绕开内网自签证书不被信任的问题
    endpoint: oss-cn-beijing-youzheng-d01-a.ops.2020yzcloud.cn
    access-key-id: woLBmYcHyTUmcliD
    access-key-secret: 4PFQc60NSg2MMIREbwqMNSQOwUOpB9
    bucket-name: oss-zhxf-app-web-1
    # 对外播放地址前缀：入库的 playUrl 和 m3u8 里的切片地址都用它。
    # 播放走 https（浏览器信任内网 CA，JVM 不信任，所以上传侧才用 http）。
    # {bucket}.{endpoint} 与 SDK 上传时实际请求的地址一致。
    # 若内网网关是 CNAME 绑定（不需要 bucket 前缀），去掉 bucket 前缀即可。
    public-base-url: https://oss-zhxf-app-web-1.oss-cn-beijing-youzheng-d01-a.ops.2020yzcloud.cn
spring:
  datasource:
    url: jdbc:mysql://10.5.51.162:3306/iot?useSSL=false&serverTimezone=UTC
    username: root
    password: Gzzhxf@2025
    driver-class-name: com.mysql.cj.jdbc.Driver
